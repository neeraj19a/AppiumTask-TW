# App Binaries Directory

Place your mobile application binaries in this directory.

## Android
- Place your `.apk` files here
- Update the `androidAppLocation` property in `src/main/resources/config.properties`
